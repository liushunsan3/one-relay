# One-Relay 使用说明

> 聚合多个 OpenAI 兼容中转站为一个本地端点：双击就跑，AI 助手帮你配置，废站自动踢出局，永远走最快的站。

---

## 系统要求

- **操作系统**：Windows 10 / 11（托盘功能依赖 Windows）
- **Node.js**：任意现代版本（下载 https://nodejs.org）
- **网络**：本机运行的代理，无需公网 IP

## 安装

1. 安装 [Node.js](https://nodejs.org)（一路默认即可）
2. 解压本包到任意目录（路径不要带空格和中文，如 `C:\one-relay\`）
3. 双击 `启动路由代理.bat`（或右键 `start-hidden.vbs` → 打开）

右下角出现蓝色「路」托盘图标，表示启动成功。双击图标打开管理面板。

## 客户端接入

| 配置项 | 值 |
|---|---|
| Base URL | `http://127.0.0.1:3099/v1` |
| API Key | `sk-router`（可在 settings.json 中修改） |

在 ZCode、OpenCat、ChatBox 等任何 OpenAI 兼容客户端中按此配置，即可使用聚合后的所有模型。

## 快速上手

### 1. 配置 AI 助手（推荐，但非必须）
打开管理面板 → 「AI 助手」页 → 填写一个 OpenAI 兼容 API 的地址、Key 和模型 → 保存。之后所有配置都可以通过对话完成。

没有助手 API？也可以手动配置（见 3. 手动加站）。

### 2. 加第一个中转站
**使用 AI 助手（推荐）**：在对话框贴入：`https://api.example.com key sk-xxxxxx`，AI 会自动查出该站支持的模型，生成配置提案，你点「应用」即生效。

**手动添加**：到「Provider 管理」页 → 新增 → 填入地址和 Key → 点「自动获取模型列表」→ 保存。

### 3. 验证模型可用
新站加入后会自动探活。到「总览」页查看各站健康状态和推荐模型列表。到「AI 助手」页说"测测哪些模型稳定"可自动实测。

## 功能说明

### 智能路由
系统自动为每个站评分（实时探活延迟 + 当日请求成功率），请求永远走评分最高的站。慢站自动让位，无需手动调优先级。

在「Provider 管理」页可一键关闭智能路由（回退到固定优先级模式）。

### 废站自动踢
探活连续 3 次失败 + 复验确认真死 → 自动踢出局（不再探测），托盘通知 + 面板红徽章。手动启用即可恢复探测。

在 settings.json 中可关闭自动踢（`autoKick: false`）。

### 自动故障转移
请求失败时自动切换下一个站，支持的切换条件：
- 401/403（Key 无效）、402（欠费）、404（模型不存在）、424（上游失败）、429（限流）
- 200 但空响应（choices: null）
- 超时、网络错误
- 包含 "quota"、"balance"、"额度"、"余额"、"限流" 等关键词的 5xx 错误

### AI 助手（8 个工具）
| 你说的话 | 功能 |
|---|---|
| "新站 https://xxx key sk-xxx" | 解析资源→自动查模型→提案→确认应用 |
| "看看现在什么情况" | 查服务状态/不可用站/推荐模型 |
| "把所有站测一遍" | 探测全部站点连通性 |
| "测测 grok" | 单站延迟测试 |
| "今天用了多少" | 用量统计（今天/7天/全部） |
| "刚才为啥失败" | 查最近日志排错 |
| "测测哪些模型稳定" | 对有多站冗余的模型实测 2 次，选出真稳的 |
| "把 modelscope 优先级置顶" | 调整站点优先级 |
| "停用 xinyue" | 启用/停用某站（配置保留可逆） |

AI 助手使用独立 API（不占用聚合池的额度），Key 在对话中自动占位符保护，不会发送给模型。

### 管理面板（5 个页签）
1. **总览**：服务状态、接入信息、推荐模型、可用模型搜索、各站健康表
2. **Provider 管理**：新增/编辑/删除站、智能路由开关、一键清理废站、评分列（进度条+明细）、探活/测试
3. **AI 助手**：对话配置（支持流式回复中断）、记忆管理、助手 API 配置
4. **用量统计**：模型 Token 占比环形图、按站/按模型的两张统计表、请求历史明细
5. **日志**：实时滚动、关键词过滤、自动滚动

## 配置说明

### providers.json（站点配置）
```json
[
  {
    "name": "myapi",           // 唯一标识名
    "baseUrl": "https://xxx/v1",  // API 地址
    "key": "sk-xxx",           // API Key
    "models": ["model-a"],     // 该站支持的模型列表
    "aliases": { "gpt": "model-a" }, // 统一别名 → 真实模型名
    "enabled": true            // 是否启用（false 时路由跳过此站）
  }
]
```
首次运行自动生成空配置（空数组），可在面板中配置，也可直接编辑此文件（热加载约 2 秒生效，修改前建议备份）。

### settings.json（全局设置）
```json
{
  "apiKey": "sk-router",        // 对外 API Key
  "priority": ["站名", "站名"],  // 固定优先级（智能路由关闭时生效）
  "assistant": { "baseUrl": "", "key": "", "model": "" }, // AI 助手独立 API
  "probeIntervalMin": 30,       // 探活间隔（分钟）
  "autoKick": true,             // 废站自动踢
  "smartRouting": true          // 智能路由开关
}
```
修改后热加载约 2 秒生效。Key 类字段留空或填入 `***` 表示保持原值。注意：手动编辑此文件时 assistant 字段需写全三项（baseUrl、key、model），否则热加载会丢失未写的项。

## 文件说明

| 文件 | 作用 |
|---|---|
| `router.js` | 路由核心（故障转移/评分/自动踢/管理 API） |
| `supervisor.js` | 看护进程（自动重启/僵死检测/心跳） |
| `tray.ps1` | 托盘图标（**UTF-8 BOM**，勿用记事本另存） |
| `start-hidden.vbs` | 无窗口启动（自动探测 node 路径） |
| `public/` | 网页控制台（原生三件套，5 页签） |
| `providers.json` | 站点配置（首次运行自动生成空模板；.bak×3 轮换备份） |
| `settings.json` | 全局设置（热加载） |
| `memory.json` | AI 助手记忆库（自动管理） |
| `stats.json` | 用量统计（按天分桶，自动生成） |
| `logs/` | 运行日志，按天滚动保留 7 天 |
| `providers.example.json` | providers.json 格式参考模板 |
| `icon.ico` | 桌面快捷方式图标 |

## 常见问题

**Q: 双击启动没反应？**
A: 大概率已在运行（单实例保护，重复启动会弹窗提示后退出）。如果确认没运行，请检查是否已安装 Node.js。

**Q: 服务连续重启？**
A: 可能是 providers.json 或 settings.json 配置格式错误导致启动崩溃。检查并修复配置后，在托盘菜单选「重启服务」即可恢复。目录下有 .bak / .bak1 / .bak2 三份轮换备份可恢复。

**Q: 怎么关闭智能路由？**
A: 在「Provider 管理」页点击「⚡智能路由」按钮，或修改 settings.json 中 `smartRouting: false`。

**Q: 怎么关闭自动踢站？**
A: 修改 settings.json 中 `autoKick: false`。

**Q: 如何重新拉取某个站的模型列表？**
A: 在「Provider 管理」页编辑该站 → 点「自动获取模型列表」按钮。

**Q: 托盘图标消失了但服务还在？**
A: 托盘进程可能异常退出，服务不受影响。双击「启动路由代理.bat」重新拉取托盘即可。

**Q: AI 助手回复怎么中断？**
A: 回复中点击红色的「⏹ 停止」按钮，已生成的内容会保留。

## 安全说明

- 服务仅监听 `127.0.0.1`（仅本机可访问），且管理 API 校验 Host/Origin 头（防网页跨站攻击）
- 所有接口不返回完整 Key（只显示尾 4 位）
- AI 助手对话中 Key 自动替换为占位符，不发送给模型
- 日志永不记录 Key 明文
- 配置写入采用原子操作（写 .tmp 再 rename，防断电损坏）
- 辅助功能（评分/自动踢/通知）全部 try/catch 包裹，异常不影响主路由